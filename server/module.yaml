product: jvm/app

dependencies:
  - ../protocol
  - $ktor.serialization.kotlinx.json
  - $ktor.server.core
  - $ktor.server.netty
  - $ktor.server.cors
  - $ktor.server.auth
  - $ktor.server.contentNegotiation
  - $ktor.server.compression
  - $ktor.server.statusPages
  - $libs.bcrypt

settings:
  ktor: enabled
  jvm:
    mainClass: de.fabiexe.spind.server.MainKt